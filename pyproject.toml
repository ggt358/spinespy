[project]
name = "postureguard"
version = "0.1.0"
description = "AI-powered posture & focus monitor for macOS menubar"
requires-python = ">=3.9"

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
